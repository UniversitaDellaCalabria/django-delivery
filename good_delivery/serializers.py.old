from rest_framework import serializers

from . models import *


class DeliveryCampainSerializer(serializers.ModelSerializer):
    class Meta:
        model = DeliveryCampain
        # Todo add @properties to Model to hanlde childs elements
        # fields = ['user', 'delivery_point',]
        fields = '__all__'


class UserDeliveryPointSerializer(serializers.ModelSerializer):
    class Meta:
        model = UserDeliveryPoint
        # Todo add @properties to Model to hanlde childs elements
        # fields = ['user', 'delivery_point',]
        fields = '__all__'


class GoodDeliverySerializer(serializers.ModelSerializer):
    class Meta:
        model = GoodDelivery
        # Todo add @properties to Model to hanlde childs elements
        # fields = ['user', 'delivery_point',]
        fields = '__all__'
